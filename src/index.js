var BridgeGame = (() => {
    var initModule =  ($container) => {
        $container.html(
            '<h1>'+
            'Witaj'+
            '</h1>'
            
        )
    }
    return {initModule: initModule}
})()